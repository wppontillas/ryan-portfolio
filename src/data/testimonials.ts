export interface Testimonial {
  quote: string;
  name: string;
  role: string;
}

// Add real client testimonials here once available — the Testimonials
// section on the homepage renders automatically as soon as this is non-empty.
export const testimonials: Testimonial[] = [];
